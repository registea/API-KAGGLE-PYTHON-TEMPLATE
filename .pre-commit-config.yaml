# used with the precommit package if you want to have black formatting as a pre-commit hook for git, which you do!
files: ^src
repos:
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
    -   id: trailing-whitespace
    -   id: end-of-file-fixer
    -   id: check-yaml
    -   id: check-added-large-files
        args: ['--maxkb=2671']

-   repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.10
    hooks:
    # Run the linter.
    -   id: ruff
        args: [ --fix ]
    # Run the formatter.
    -   id: ruff-format

-   repo: https://github.com/pre-commit/mirrors-mypy
    rev: 'v1.19.1'
    hooks:
    -   id: mypy
        args: [--ignore-missing-imports]
        stages: [pre-commit]
        additional_dependencies: ['types-requests', 'types-PyYAML']

# this is where we'd include gitleaks, but currently we can't do it without disabling SSL due to Mars' excellent
# security policies of man-in-the-middling our OWN ssl connections
#-   repo: https://github.com/gitleaks/gitleaks
#    rev: v8.16.1
#    hooks:
#      - id: gitleaks
